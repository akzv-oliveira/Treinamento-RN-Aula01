import React from 'react';
import {ScheduleView, ScheduleText} from './styles.Schedule';

export default function ScheduleScreen() {
  return (
    <ScheduleView>
      <ScheduleText>Schedule Screen!</ScheduleText>
    </ScheduleView>
  );
}
